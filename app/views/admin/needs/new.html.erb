<%= form_for [:admin, @organization, @need] do |f| %>
  <p>
    <%= f.label :item %>
    <%= f.text_field :item %>
  </p>

  <p>
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </p>

  <%= f.button 'Submit' %>
<% end %>
